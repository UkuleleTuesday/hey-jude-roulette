{
  "name": "hey-jude-roulette",
  "private": true,
  "description": "Browser tests for the wheel. The app itself is a single dependency-free index.html — this manifest exists only so CI has something to install.",
  "scripts": {
    "test": "node tests/run.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
